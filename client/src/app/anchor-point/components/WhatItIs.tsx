import Section from "@/components/Section";

export default function WhatItIs() {
  return (
    <Section>
      <p className="mx-auto max-w-2xl text-center text-lg leading-relaxed text-muted">
        Anchor Point is a recurring meetup where developers, founders, and
        ecosystem leaders build on Stellar together. It&apos;s a fixed point you
        come back to — not a one-off event, but a regular gathering for the
        people shaping the ecosystem.
      </p>
    </Section>
  );
}
